package com.currency.currencyconversionservice.controller;

import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

import com.currency.currencyconversionservice.entity.CurrencyConverted;

@RestController
public class ConversionController {
	
	@GetMapping("/getConvertedValue/from/{from}/to/{to}")
	public ResponseEntity<CurrencyConverted> getConvertedValue(){
		
		return new ResponseEntity<CurrencyConverted>(null);
	}
	
	@GetMapping("/test")
	public String test() {
		return "Congratulations, It is Working!!";
	}

}
